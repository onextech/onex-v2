export type SectionButton = {
  title: string;
  label: string;
  href: string;
};

export type SectionBlock = {
  title: string;
  heading: string;
  description: string;
  button: SectionButton;
};
